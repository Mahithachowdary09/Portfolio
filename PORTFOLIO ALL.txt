HTML: line 5
CSS: line 229
JS: line 355

HTML:
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>K. Mahitha — Java Full-Stack Developer</title>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

  <!-- Icons -->
  <link rel="stylesheet" href="https://unicons.iconscout.com/release/v4.0.8/css/line.css">

  <!-- CSS -->
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <!-- HEADER / NAV -->
  <header>
    <div class="container nav">
      <h1 class="logo">K. <span>Mahitha</span></h1>

      <nav>
        <ul class="nav-links">
          <li><a class="nav-link active" href="#home">Home</a></li>
          <li><a class="nav-link" href="#about">About</a></li>
          <li><a class="nav-link" href="#skills">Skills</a></li>
          <li><a class="nav-link" href="#internships">Experience</a></li>
          <li><a class="nav-link" href="#projects">Projects</a></li>
          <li><a class="nav-link" href="#education">Education</a></li>
          <li><a class="nav-link" href="#contact">Contact</a></li>
        </ul>

        <div class="nav-toggle" aria-label="Open menu" title="Menu">
          <i class="uil uil-bars"></i>
        </div>
      </nav>
    </div>
  </header>

  <!-- HOME -->
  <section id="home" class="home">
    <div class="container home-content">
      <div class="home-text reveal">
        <h3>Hello, I'm</h3>
        <h2 class="name"><span class="highlight">KANCHARLA VENKATA SAI MAHITHA CHOWDARY</span></h2>
        <h3 class="typing"></h3>

        <p>
          Java Full-Stack Intern (B.Tech Electronics, RISE Krishna Sai Prakasam Group). Currently interning at
          <strong>Megan Soft Pvt. Ltd., Chennai</strong> — working on backend modules with Java, Spring Boot and REST APIs.
        </p>

        <div class="home-btns">
          <a class="btn" href="#contact">Hire Me</a>
          <a class="btn-outline" href="Mahitha_Resume.pdf" download>Download CV</a>
        </div>

        <div class="home-social">
          <a href="https://www.linkedin.com/in/mahitha-kancharla-301880266" target="_blank" title="LinkedIn"><i class="uil uil-linkedin"></i></a>
          <a href="https://github.com/Mahithachowdary09" target="_blank" title="GitHub"><i class="uil uil-github"></i></a>
        </div>
      </div>

      <div class="home-image reveal">
        <!-- placeholder: replace mahitha.jpg with actual photo -->
        <img src="profile.jpg" alt="K. Mahitha" class="profile-img">
      </div>
    </div>
  </section>

  <!-- ABOUT -->
  <section id="about" class="about container">
    <h2 class="section-title reveal">About Me</h2>
    <p class="reveal">
      I am a B.Tech student in Electronics (2021–2025) at RISE Krishna Sai Prakasam Group of Institutions.
      Passionate about building reliable software and applying Java backend skills to real-world problems.
      I enjoy learning full-stack development, firmware prototyping with Arduino, and contributing to team projects.
    </p>

    <div class="contact-card reveal">
      <h3>Career Objective</h3>
      <p>To secure a challenging position in a dynamic organization where I can leverage my communication, problem-solving and technical skills to contribute to growth while continuing my professional development.</p>
    </div>
  </section>

  <!-- SKILLS -->
  <section id="skills" class="skills container">
    <h2 class="section-title reveal">Skills</h2>

    <div class="skills-grid reveal">
      <div class="skill-card interactive-box">
        <h3>Programming</h3>
        <p>Java · Python</p>
      </div>

      <div class="skill-card interactive-box">
        <h3>Frameworks & Web</h3>
        <p>Spring Boot · REST APIs · HTML · CSS · JavaScript</p>
      </div>

      <div class="skill-card interactive-box">
        <h3>Databases</h3>
        <p>SQL · MySQL / MariaDB</p>
      </div>

      <div class="skill-card interactive-box">
        <h3>Tools & Embedded</h3>
        <p>Arduino · MATLAB · Git · Postman</p>
      </div>

      <div class="skill-card interactive-box">
        <h3>Professional</h3>
        <p>Communication · Teamwork · Time management</p>
      </div>
    </div>
  </section>

  <!-- INTERNSHIPS / EXPERIENCE -->
  <section id="internships" class="experience container">
    <h2 class="section-title reveal">Experience</h2>

    <div class="timeline reveal">
      <div class="timeline-item">
        <h3>Megan Soft Pvt. Ltd., Chennai (Nov 17 – Present)</h3>
        <h4>Java Full-Stack Intern | Full-time</h4>
        <p>Working on backend modules using Java and Spring Boot. Building RESTful APIs, writing database queries, debugging issues, and supporting frontend integration.</p>
        <ul class="small-list">
          <li>Implemented API endpoints and assisted with integration testing.</li>
          <li>Collaborated with the team on feature design and debugging.</li>
          <li>Gained exposure to professional development workflows.</li>
        </ul>
      </div>

      <div class="timeline-item">
        <h3>College Projects & Volunteering</h3>
        <h4>Developer & Organizer</h4>
        <p>Worked on academic projects, organized workshops and participated in tech events and quizzes.</p>
      </div>
    </div>
  </section>

  <!-- PROJECTS -->
  <section id="projects" class="projects container">
    <h2 class="section-title reveal">Projects</h2>

    <div class="project-grid reveal">
      <div class="project-card">
        <h3>Speech Recognition System</h3>
        <p><strong>MATLAB · Arduino</strong> — Developed an Automatic Speech Recognition (ASR) prototype that converts spoken commands into text for control applications. Focused on signal processing and basic recognition pipelines.</p>
        <p class="muted">Key contributions: feature extraction, prototype integration and testing.</p>
      </div>

      <div class="project-card">
        <h3>Mini Web Apps</h3>
        <p>Built small responsive pages and demos using HTML, CSS and JavaScript for coursework and hands-on practice.</p>
      </div>

      <div class="project-card">
        <h3>Workshop & Events</h3>
        <p>Organized sessions on leadership, time-management and presentation skills, and participated in college tech expos.</p>
      </div>
    </div>
  </section>

  <!-- EDUCATION -->
  <section id="education" class="education container">
    <h2 class="section-title reveal">Education</h2>

    <ul class="edu-list reveal">
      <li><strong>B.Tech (Electronics)</strong> — RISE Krishna Sai Prakasam Group of Institutions (2021–2025) • CGPA: 7.3</li>
      <li><strong>Intermediate (MPC)</strong> — BR Oxford College (2019–2021) • Percentage: 75%</li>
      <li><strong>Secondary (SSC)</strong> — Bhashyam Public School (2019) • GPA: 9.2</li>
    </ul>

    <h3 class="section-subtitle reveal">Certifications</h3>
    <ul class="cert-list reveal">
      <li>Python & Web Development — Brahmas Academy</li>
      <li>VLSI Domain Certification — Naresh Technologies</li>
    </ul>
  </section>

  <!-- CONTACT -->
  <section id="contact" class="contact container">
    <h2 class="section-title reveal">Contact</h2>

    <p class="reveal"><i class="uil uil-phone"></i> 6300060695</p>
    <p class="reveal"><i class="uil uil-envelope"></i> mahithakancharla09@gmail.com</p>

    <div class="social-links reveal">
      <a href="https://www.linkedin.com/in/mahitha-kancharla-301880266" target="_blank" title="LinkedIn"><i class="uil uil-linkedin"></i></a>
      <a href="https://github.com/Mahithachowdary09" target="_blank" title="GitHub"><i class="uil uil-github"></i></a>
    </div>

    <div class="contact-note reveal">
      <p><strong>Open to:</strong> Internships, Full-time roles, and collaborative projects.</p>
    </div>

    <div class="contact-card reveal" style="margin-top:18px;">
      <h3>Personal Details</h3>
      <p>Address: 2-57 Ramalayam Street, Nekunampuram (V), Kandukur, Ongole City, 523113, Andhra Pradesh</p>
      <p>Languages: Telugu, English</p>
      <p>Hobbies: Reading, Cooking</p>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <p>© <span id="year"></span> <span class="highlight">K. Mahitha</span>. All Rights Reserved.</p>
  </footer>

  <!-- BACK TO TOP -->
  <button class="back-to-top" aria-label="Back to top">
    <i class="uil uil-arrow-up"></i>
  </button>

  <!-- JS -->
  <script src="script.js"></script>
</body>
</html>


CSS:


/* ===== RESET & GLOBAL ===== */
* { margin:0; padding:0; box-sizing:border-box; font-family: "Poppins", sans-serif; }

:root{
  --bg:#030103;
  --bg-alt:#070307;
  --card:#0d0610;
  --violet:#9b59ff;      /* neon violet */
  --violet-strong:#7a39e6;
  --violet-soft: rgba(155,89,255,0.08);
  --text:#f3f1fa;
  --muted:#cfc9e8;
  --border: rgba(155,89,255,0.06);
}

body{
  background: radial-gradient(circle at 10% 10%, #060014 0%, var(--bg) 40%);
  color:var(--text);
  scroll-behavior:smooth;
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
}

/* CONTAINER */
.container{ width:92%; max-width:1100px; margin:0 auto; }

/* NAV */
header{
  position:sticky; top:0; z-index:120; background: rgba(3,1,6,0.6);
  backdrop-filter: blur(6px); border-bottom: 1px solid rgba(122,57,230,0.05);
}
.nav{ display:flex; justify-content:space-between; align-items:center; padding:14px 0; }
.logo{ font-size:22px; font-weight:700; color:var(--violet); }
.logo span{ color:var(--text); margin-left:6px; font-weight:600; }

nav{ display:flex; align-items:center; gap:20px; }
.nav-links{ display:flex; gap:22px; list-style:none; }
.nav-links a{ color:var(--muted); text-decoration:none; font-weight:500; padding-bottom:6px; position:relative; }
.nav-links a::after{ content:""; position:absolute; left:0; bottom:0; width:0; height:2px; background:var(--violet); transition:width .25s; border-radius:999px; }
.nav-links a:hover, .nav-links a.active{ color:var(--text); }
.nav-links a:hover::after, .nav-links a.active::after{ width:100%; }

.nav-toggle{ display:none; font-size:24px; color:var(--text); cursor:pointer; }

/* HOME */
.home{ padding:80px 0 60px; min-height:calc(100vh - 70px); display:flex; align-items:center; }
.home-content{ display:grid; grid-template-columns: 3fr 2fr; gap:36px; align-items:center; }

.home-text h3{ font-weight:300; font-size:16px; color:var(--muted); margin-bottom:6px; }
.name{ font-size:40px; color:var(--violet); margin:6px 0; }
.typing{ font-size:18px; color:var(--violet-strong); min-height:28px; margin-bottom:12px; }
.home-text p{ color:var(--muted); line-height:1.6; max-width:700px; }

.home-btns{ margin-top:18px; display:flex; gap:12px; flex-wrap:wrap; }
.btn, .btn-outline{ padding:10px 20px; border-radius:999px; text-decoration:none; font-weight:600; font-size:14px; transition:all .25s; display:inline-block; }
.btn{ background:var(--violet); color:#0b0b0b; box-shadow:0 8px 26px rgba(122,57,230,0.12); }
.btn-outline{ border:1px solid var(--violet); color:var(--violet); background:transparent; }
.btn:hover{ transform:translateY(-3px); box-shadow:0 14px 40px rgba(122,57,230,0.16); }
.btn-outline:hover{ background:var(--violet-soft); }

.home-social{ margin-top:16px; display:flex; gap:12px; }
.home-social a{ width:36px; height:36px; display:inline-flex; align-items:center; justify-content:center; border-radius:50%; border:1px solid var(--violet-soft); color:var(--violet); font-size:18px; transition:all .2s; }
.home-social a:hover{ background:var(--violet); color:#060606; transform:translateY(-4px); }

/* PROFILE IMAGE */
.home-image{ display:flex; justify-content:center; }
.profile-img{ width:240px; height:240px; object-fit:cover; border-radius:50%; border:3px solid var(--violet); box-shadow:0 0 30px rgba(122,57,230,0.16); background:#000; }

/* SECTION TITLES */
.section-title{ text-align:center; font-size:28px; font-weight:700; color:var(--violet); margin:60px 0 18px; position:relative; }
.section-title::after{ content:""; display:block; width:60px; height:3px; background:var(--violet); margin:10px auto 0; border-radius:999px; }

.section-subtitle{ text-align:center; color:var(--muted); margin-bottom:12px; }

/* CARDS & GRIDS */
.interactive-box, .skill-card, .timeline-item, .project-card{ background:var(--card); border:1px solid var(--border); padding:18px; border-radius:12px; box-shadow:0 8px 20px rgba(0,0,0,0.6); transition:transform .25s, box-shadow .25s, border-color .25s; }
.interactive-box:hover, .skill-card:hover, .timeline-item:hover, .project-card:hover{ transform:translateY(-6px); border-color:var(--violet); box-shadow:0 18px 48px rgba(122,57,230,0.06); }

.skills-grid, .project-grid, .timeline, .edu-list{ display:grid; gap:18px; }
.skills-grid{ grid-template-columns: repeat(auto-fit, minmax(220px,1fr)); margin-bottom:20px; }
.project-grid{ grid-template-columns: repeat(auto-fit, minmax(260px,1fr)); margin-bottom:20px; }
.timeline{ grid-template-columns:1fr; margin-bottom:20px; }

.skill-card h3, .project-card h3, .timeline-item h3{ color:var(--text); margin-bottom:8px; }
.timeline-item h4{ color:var(--violet); margin-bottom:8px; }
.skill-card p, .project-card p, .timeline-item p{ color:var(--muted); font-size:14px; line-height:1.6; }
.small-list{ margin-top:8px; color:var(--muted); font-size:14px; list-style:disc; padding-left:18px; }

/* EDUCATION & CERTS */
.edu-list{ list-style:none; max-width:900px; margin:0 auto 20px; padding:10px; }
.edu-list li, .cert-list li{ color:var(--muted); margin-bottom:8px; }

/* CONTACT */
.contact p{ text-align:center; color:var(--muted); margin:8px 0; }
.contact i{ color:var(--violet); margin-right:8px; }

.social-links{ display:flex; gap:12px; justify-content:center; margin-top:12px; }
.social-links a{ width:40px; height:40px; display:inline-flex; align-items:center; justify-content:center; border-radius:8px; border:1px solid var(--violet-soft); color:var(--violet); text-decoration:none; }

/* FOOTER */
footer{ text-align:center; padding:18px 0 30px; color:var(--muted); margin-top:40px; }

/* BACK TO TOP */
.back-to-top{ position:fixed; right:24px; bottom:24px; width:42px; height:42px; border-radius:50%; background:var(--violet); border:none; color:#060606; display:flex; align-items:center; justify-content:center; box-shadow:0 10px 24px rgba(0,0,0,0.6); opacity:0; pointer-events:none; transition:opacity .25s, transform .2s; z-index:140; }
.back-to-top.show{ opacity:1; pointer-events:auto; transform:translateY(0); }

/* REVEAL ANIMATION */
.reveal{ opacity:0; transform:translateY(18px); transition:opacity .6s ease, transform .6s ease; }
.reveal.show{ opacity:1; transform:translateY(0); }

/* Minor responsive tweaks */
@media (max-width:900px){
  .home-content{ grid-template-columns:1fr; text-align:center; }
  .home-text{ order:2; }
  .home-image{ order:1; margin-bottom:14px; }
  .nav-links{ position:absolute; top:64px; right:0; background:var(--bg-alt); flex-direction:column; width:220px; padding:16px; transform:translateX(120%); transition:transform .3s; box-shadow:-6px 10px 30px rgba(0,0,0,0.9); }
  .nav-links.open{ transform:translateX(0); }
  .nav-toggle{ display:block; }
}




JS:


document.addEventListener("DOMContentLoaded", () => {
  // Elements
  const navToggle = document.querySelector(".nav-toggle");
  const navLinksContainer = document.querySelector(".nav-links");
  const navLinks = document.querySelectorAll(".nav-link");
  const sections = document.querySelectorAll("section[id]");
  const revealEls = document.querySelectorAll(".reveal");
  const backToTop = document.querySelector(".back-to-top");
  const typingEl = document.querySelector(".typing");
  const yearEl = document.getElementById("year");

  // Footer year
  if (yearEl) yearEl.textContent = new Date().getFullYear();

  // NAV TOGGLE (mobile)
  if (navToggle && navLinksContainer) {
    navToggle.addEventListener("click", () => {
      navLinksContainer.classList.toggle("open");
    });
    navLinks.forEach(l => l.addEventListener("click", () => navLinksContainer.classList.remove("open")));
  }

  // Smooth scroll for nav links
  navLinks.forEach(link => {
    link.addEventListener("click", (e) => {
      const target = link.getAttribute("href");
      if (target && target.startsWith("#")) {
        e.preventDefault();
        document.querySelector(target).scrollIntoView({ behavior: "smooth" });
      }
    });
  });

  // Active nav item on scroll & back-to-top show
  window.addEventListener("scroll", () => {
    const scrollY = window.pageYOffset;

    sections.forEach(section => {
      const top = section.offsetTop - 120;
      const height = section.offsetHeight;
      const id = section.getAttribute("id");
      if (scrollY >= top && scrollY < top + height) {
        navLinks.forEach(link => link.classList.remove("active"));
        const active = document.querySelector(`.nav-link[href="#${id}"]`);
        if (active) active.classList.add("active");
      }
    });

    if (scrollY > 400) backToTop.classList.add("show");
    else backToTop.classList.remove("show");

    // reveal elements (fallback for browsers without IntersectionObserver)
    revealEls.forEach(el => {
      const rect = el.getBoundingClientRect();
      if (rect.top < window.innerHeight - 80) el.classList.add("show");
    });
  });

  // Back to top click
  if (backToTop) backToTop.addEventListener("click", () => window.scrollTo({ top: 0, behavior: "smooth" }));

  // IntersectionObserver for reveal (preferred)
  if ("IntersectionObserver" in window) {
    const io = new IntersectionObserver((entries, obs) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add("show");
          obs.unobserve(entry.target);
        }
      });
    }, { threshold: 0.18 });

    revealEls.forEach(el => io.observe(el));
  }

  // Typing effect (role-focused)
  const texts = [
    "Java Full-Stack Intern",
    "Spring Boot · REST APIs · SQL",
    "Arduino · MATLAB · Prototyping",
    "Frontend Integration · JavaScript"
  ];
  let tIndex = 0, chIndex = 0, deleting = false;

  function typeLoop() {
    if (!typingEl) return;
    const current = texts[tIndex];
    if (!deleting) {
      typingEl.textContent = current.substring(0, chIndex + 1);
      chIndex++;
      if (chIndex === current.length) {
        deleting = true;
        setTimeout(typeLoop, 900);
        return;
      }
    } else {
      typingEl.textContent = current.substring(0, chIndex - 1);
      chIndex--;
      if (chIndex === 0) {
        deleting = false;
        tIndex = (tIndex + 1) % texts.length;
      }
    }
    setTimeout(typeLoop, deleting ? 40 : 90);
  }
  typeLoop();
});
